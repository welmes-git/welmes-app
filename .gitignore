# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supplier import session (login cookies)
scripts/.sd-session.json

# Supabase CLI local state (linked project ref, downloaded CLI metadata)
supabase/.temp/

# Environment files — never commit (set production values in the Vercel dashboard)
.env
.env.*
!.env.example
scripts/.tmp-*

# Local refresh backups (pre-change image/description snapshots, product data)
scripts/.image-backup-*.json
scripts/.desc-backup-*.json
